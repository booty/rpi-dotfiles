echo ".zshrc loading, bro"

# ----[ Apt stuff ]----

alias a.up="sudo apt update && sudo apt upgrade -y && sudo apt full-upgrade -y && sudo apt autoremove -y && sudo apt autoclean -y"

# ----[ Hailo ]----

alias h.diag="hailortcli fw-control identify"
